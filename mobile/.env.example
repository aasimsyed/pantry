# Copy to .env and fill in. For EAS/TestFlight, set these in EAS Secrets instead.
# Sentry DSN (get from Sentry → Project Settings → Client Keys). Leave empty to disable crash reporting.
EXPO_PUBLIC_SENTRY_DSN=
