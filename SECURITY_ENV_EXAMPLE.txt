# Security Configuration for Smart Pantry API
# Copy these to your .env file

# JWT Authentication
SECRET_KEY=your-secret-key-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# Rate Limiting (optional - defaults are reasonable)
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_PER_MINUTE=100
# RATE_LIMIT_AUTH_PER_MINUTE=5

# Existing API Keys (keep your existing values)
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
AI_PROVIDER=openai
AI_MODEL=gpt-4-turbo-preview

# Database (keep your existing values)
DATABASE_URL=sqlite:///pantry.db
# Or for PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/pantry

# To generate a secure SECRET_KEY, run:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or:
# openssl rand -hex 32

