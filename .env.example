# Smart Pantry – Environment Variables
# Copy to .env and replace with your values.
# Generate SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -hex 32

# -----------------------------------------------------------------------------
# Security & Auth (required for API)
# -----------------------------------------------------------------------------
SECRET_KEY=your-secret-key-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# SQLite (simplest, but differs from production):
# DATABASE_URL=sqlite:///pantry.db

# Local Postgres via Docker (matches production; recommended):
#   ./scripts/start-db-local.sh   # starts Postgres on 5433 (avoids system Postgres on 5432)
#   Then set:
DATABASE_URL=postgresql://pantry_user:pantry_pass@localhost:5433/pantry_db

# -----------------------------------------------------------------------------
# AI (OpenAI and/or Anthropic)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=your-openai-api-key-here
ANTHROPIC_API_KEY=your-anthropic-api-key-here
AI_PROVIDER=openai
AI_MODEL=gpt-4-turbo-preview

# -----------------------------------------------------------------------------
# OCR (Google Vision or Tesseract)
# -----------------------------------------------------------------------------
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/google-credentials.json
# Or: GOOGLE_VISION_API_KEY=your-vision-api-key
OCR_PREFERRED_BACKEND=google
OCR_CONFIDENCE_THRESHOLD=0.85

# -----------------------------------------------------------------------------
# Email (SMTP for password reset, notifications)
# -----------------------------------------------------------------------------
# Gmail SMTP (free, 500 emails/day):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-gmail@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_FROM_EMAIL=your-gmail@gmail.com
SMTP_FROM_NAME="Smart Pantry"
# Deep link URL for password reset (mobile app)
# For production: smartpantry://
# For web frontend: https://your-domain.com
FRONTEND_URL=smartpantry://

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------
# TESSERACT_CMD=tesseract
# OCR_CACHE_ENABLED=true
# OCR_CACHE_DIR=./cache/ocr
# AI_CACHE_ENABLED=true
# AI_CACHE_DIR=./cache/ai
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_PER_MINUTE=100
# LOG_LEVEL=INFO
# CORS_ORIGINS=http://localhost:5173

# -----------------------------------------------------------------------------
# Instacart Integration (for shopping list features)
# -----------------------------------------------------------------------------
# Get API key from: https://dashboard.instacart.com/
# INSTACART_API_KEY=your-instacart-api-key-here
# INSTACART_ENABLED=true
# INSTACART_API_URL=https://connect.instacart.com
# INSTACART_DEV_API_URL=https://connect.dev.instacart.tools
# INSTACART_TIMEOUT=30
# INSTACART_LINK_EXPIRES_DAYS=30

# Error tracking (production): Sentry – set SENTRY_DSN to send errors to sentry.io
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# SENTRY_ENVIRONMENT=production
